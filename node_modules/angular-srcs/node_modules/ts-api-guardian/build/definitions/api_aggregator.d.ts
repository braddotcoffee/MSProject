import * as ts from 'typescript';
export declare abstract class Base<T> {
    abstract mapNode(n: ts.Node): T;
    mapNodes(nodes: ts.Node[]): T[];
}
export default class PublicApiAggregator extends Base<string[]> {
    mapNode(node: ts.Node): string[];
    private shouldBeSkipped(decl);
    private getFunctionLike(node);
    private getConstructor(node);
    private getConstructorProperties(node);
    private getGetter(node);
    private getSetter(node);
    private getProperty(decl);
    private getParameters(nodes);
    private getParameter(node);
    private getClassLike(keyword, decl);
    private getColonType(node);
}
